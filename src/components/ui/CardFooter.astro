---
interface Props {
  class?: string;
  'data-slot'?: string;
}

const {
  class: className = '',
  'data-slot': slot = 'card-footer',
  ...props
} = Astro.props;
---

<div
  class:list={[
    'flex items-center p-6 pt-0',
    className
  ]}
  data-slot={slot}
  {...props}
>
  <slot />
</div>
